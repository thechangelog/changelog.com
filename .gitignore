# App artifacts
/_build
/db
/deps
/*.ez
.DS_Store
/.elixir_ls

# Generate on crash by the VM
erl_crash.dump

# Static artifacts
/assets/node_modules
/assets/app/deprecated

# npm logs
npm-debug.log*

# Since we are building assets,
# we ignore priv/static. You may want to comment
# this depending on your deployment strategy.
/priv/static/

# Legacy assets which are built into thechangelog/proxy image
/nginx/www/wp-content

# The config/prod.secret.exs file by default contains sensitive
# data and you should not commit it into version control.
#
# Alternatively, you may comment the line below and commit the
# secrets file as long as you replace its contents by environment
# variables.
/config/prod.secret.exs
/priv/uploads
/priv/db
/import/cookies.yml
/cover

# .envrc is likely to contain user-specific secrets, such as CIRCLE_TOKEN
.envrc
# like .envrc, but for asdf
.tool-versions

# .terraform will contain large, platform-specific binaries
.terraform

# Ignore local dir where test run results are stored
# Helps identify flaky tests
/test_flakes

# Kubernetes config files, LKE to begin with
.kube

# Local binaries
/bin
