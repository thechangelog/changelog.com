#!/usr/bin/env bash
#MISE description="Bump tool versions in mise.toml"
set -euo pipefail

if [[ $# -eq 0 ]]; then
    echo "Usage: mise run bump <tool> [<tool>...]"
    echo "Example: mise run bump erlang elixir node"
    exit 1
fi

mise upgrade --bump "$@"
