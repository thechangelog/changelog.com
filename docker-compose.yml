version: '2'
services:
  db:
    image: "postgres:9.5"
  web:
    build: .
    command: "mix phoenix.server"
    environment:
      - DB_URL="postgres://postgres@localhost:5432/changelog_dev"
    volumes:
      - .:/code
    ports:
      - "4000:4000"
    depends_on:
      - db