<div class="item">
  <input name="{{parentType}}[{{attrName}}][{{index}}][podcast_id]" type="hidden" value="{{id}}">
  <input class="js-position" name="{{parentType}}[{{attrName}}][{{index}}][position]" type="hidden">
  <img class="ui tiny image" src="{{avatarUrl}}">
  <div class="content">
    <div class="header">{{name}}</div>
  </div>

  <div class="right floated content">
    <div class="ui tiny icon button js-remove"><i class="remove icon"></i></div>
  </div>
</div>