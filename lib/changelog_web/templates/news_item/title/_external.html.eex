<%=
if SharedHelpers.controller_action_combo(@conn) == "news_item-show" do
  link(raw(@item.headline <> "&nbsp;&nbsp;↦"), to: news_item_url(@item), rel: "external", data: [news: true])
else
  link(@item.headline, to: news_item_url(@item), rel: "external", data: [news: true])
end
%>
