<div class="ui basic segment">
  <h1 class="ui dividing header">
    <%= link "Podcasts", to: Routes.admin_podcast_path(@conn, :index) %>
    &raquo; Editing
    &raquo; <%= @podcast.name %>

    <%= link "(View)",
    to: Routes.podcast_path(@conn, :show, @podcast.slug),
    class: "view" %>
  </h1>
</div>

<div class="ui basic segment text container">
  <%= render("_form.html", Map.merge(assigns, %{action: Routes.admin_podcast_path(@conn, :update, @podcast.slug)})) %>
</div>
