<div class="ui basic segment">
  <h1 class="ui dividing header">
    <%= link("News", to: Routes.admin_news_item_path(@conn, :index)) %> &raquo; Comments
    <div class="sub header">no spam!</div>
  </h1>

  <div class="actions right">
  </div>
</div>

<div class="ui basic segment">
<%= if Enum.any?(@comments) do %>
  <%= render("_table.html", conn: @conn, comments: @comments) %>
  <%= render(SharedView, "_pagination.html", assigns) %>
<% end %>
</div>
