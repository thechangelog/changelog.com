<div class="ui basic segment">
  <h1 class="ui dividing header">
    News Sources matching '<%= @query %>': <%= length(@results) %>
  </h1>

<%= if Enum.any?(@results) do %>
  <%= render(NewsSourceView, "_table.html", Map.merge(assigns, %{sources: @results})) %>
<% end %>
</div>

