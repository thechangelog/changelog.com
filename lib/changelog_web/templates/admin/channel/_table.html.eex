<table class="ui celled striped table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Slug</th>
      <th>Description</th>
      <th>Episodes</th>
      <th>Posts</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
  <%= for channel <- @channels do %>
    <tr>
      <td><%= channel.name %></td>
      <td><%= channel.slug %></td>
      <td><%= truncate(channel.description, 60) %></td>
      <td><%= episode_count(channel) %></td>
      <td><%= post_count(channel) %></td>
      <td class="center aligned">
        <div class="ui small basic buttons">
          <%= link to: admin_channel_path(@conn, :edit, channel), class: "ui button" do %>
            <i class="edit icon"></i>
            Edit
          <% end %>

          <%= link to: admin_channel_path(@conn, :delete, channel), method: :delete, data: [confirm: "Are you sure?"], class: "ui button" do %>
            <i class="trash icon"></i>
            Delete
          <% end %>
        </div>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
