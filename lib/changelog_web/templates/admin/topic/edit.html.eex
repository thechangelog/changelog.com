<div class="ui basic segment text container">
  <h1 class="ui dividing header">
    <%= link "Topics", to: Routes.admin_topic_path(@conn, :index) %>
    &raquo; Edit

    <%= external_link("(View)", to: Routes.topic_path(@conn, :show, @topic.slug), class: "view") %>
  </h1>

  <%= render("_form.html", conn: @conn, changeset: @changeset, action: Routes.admin_topic_path(@conn, :update, @topic.slug)) %>
</div>
