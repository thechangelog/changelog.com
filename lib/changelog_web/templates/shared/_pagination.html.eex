<%= if @page.page_number > 1 do %>
  <nav class="pagination" role="menubar" aria-label="Pagination">
    <%= if @page.page_number > 1 do %>
      <%= link "Newer", to: current_path(@conn, %{page: @page.page_number - 1}), class: "pagination-item pagination-item--button" %>
    <% else %>
      <span class="pagination-item">&nbsp;</span>
    <% end %>
    <span class="pagination-item pagination-item--current"><%= @page.page_number %></span>
    <%= if @page.page_number < @page.total_pages do %>
      <%= link "Older", to: current_path(@conn, %{page: @page.page_number + 1}), class: "pagination-item pagination-item--button" %>
    <% end %>
  </nav>
<% end %>
