<%= greeting(@person) %>


Hot off the press! <%= @episode.title %>

<%= @episode.summary |> md_to_text()  %>


Listen here: <%= episode_url(Endpoint, :show, @episode.podcast.slug, @episode.slug) %>

Don't want these notifications? Disable 'em:

<%= PersonView.opt_out_url(Endpoint, @person, @subscription.id) %>

💚, Logbot
